# Persist packet forwarding so clients keep egressing via WARP across reboots.
# Install: sudo cp system/99-warp-routing.conf /etc/sysctl.d/ && sudo sysctl --system
#
net.ipv4.ip_forward=1
net.ipv6.conf.all.forwarding=1
